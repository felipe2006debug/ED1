/*
Nome: Felipe Novaes Silva
Matrícula: 54881
Lista: 4
Exercício: 2
*/

/*2. Em que situações uma fila pode ser utilizada? */

#include <stdio.h>
#include <string.h>
#include <stdlib.h>
int main(){
	/*
	Resposta: Uma fila pode ser utilizada quando existe a necessidade de se respeitar
	a ordem de chegada (FIFO), ou seja, quem chega primeiro deve ser atendido primeiro.
	Essa situação pode ocorrer em vários casos práticos no campo computacional, como
	no escalonamento de processos, que ocorre quando vários programas querem usar a CPU
	(Central Processing Unit) ao mesmo tempo, onde o sistema operacional irá colocá-los
	em uma fila, garantindo que o primeiro processo a solicitar a CPU será o executado;
	no gerenciamento de periféricos, como em filas de impressão, onde em um conjunto de
	documentos, o sistema criará uma fila, e a impressora processa o primeiro arquivo da
	lista enquanto os outros aguardam; em redes de computadores, os pacotes de dados
	chegam em altas velocidades, quando o trafégo é intenso, o roteador armazena esses
	pacotes em um fila e os envia um a um; e em algoritmos de busca, pois na teoria dos
	grafos, a busca em largura utiliza uma fila para visitar os nós de uma estrutura
	de forma nivelada, ao enfileirar os vizinhos de um nó, o próximo nível só pode ser
	acessado ao esvaziar a fila do nível atual.
	*/
}
